======== TODO ========

WIP
	- redo the entity manager to move logic away from component, allowing batch processing and minimizing repeated checks
	- finish the ScreenMngr draw methods
	- implement the event manager properly

To add
	- implement a Col4 type

Style

Refactor


Performance

Random bullshit go !
	- use chain_t instead of List
	- use bank_t< N > instead of Arrays
	- use matrices instead of VecN_arr_t

======== Nomenclature ========

set(   val )       : v0  = v
move(  delta )     : v0 += d
scale( factor )    : v0 *= f

push(  target, d ) : v0 += min(|d|, |t - v|) : aka move towards t by d  ( d can be negative ) //   TODO : use me or lose me
nudge( target, % ) : v0 += ( % * ( v - v0 )) : aka move towards t by x% ( x can be negative ) //   TODO : use me or lose me

======= Phylosophy ========